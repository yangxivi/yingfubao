# 应付宝 环境变量示例（复制为 .env 并填写）
# 复制本文件为 backend/.env 后修改

# JWT 签名密钥（生产环境务必改成随机长字符串）
JWT_SECRET=yingfubao-secret-key-change-in-production

# ===== OCR 引擎 =====
# 本地 Tesseract（pytesseract），零密钥、可离线、完全免费，已含图像预处理。
# 服务器需安装 tesseract 并含中文包 chi_sim（Dockerfile 已预装）。
# 无需任何额外变量；仅在 tesseract 命令缺失时返回空结构（前端转人工补录）。
